interface:
  display_name: "Agentic Design"
  short_description: "Create deliberate editable graphics"
  default_prompt: "Use $agentic-design to create a deliberate editable graphic, inspect the rendered result, and show me the preview."
  brand_color: "#315CF5"
policy:
  allow_implicit_invocation: true
